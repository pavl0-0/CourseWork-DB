@model Kino.Models.Country

@{
    ViewData["Title"] = "Деталі";
}

<div class="container" style="margin-top: 50px;">

    <div class="cinema-form-card">
        <h2 style="text-align: center; margin-bottom: 30px;">Інфо про країну</h2>

        <div style="margin-bottom: 30px;">

            <div class="form-group">
                <label class="control-label">ID</label>
                <input value="@Model.CountryId" class="form-control" readonly style="color: #b3b3b3 !important;" />
            </div>

            <div class="form-group">
                <label class="control-label">Назва країни</label>
                <input value="@Model.Name" class="form-control" readonly />
            </div>

        </div>

        <div style="text-align: center; margin-top: 30px;">
            <a asp-action="Edit" asp-route-id="@Model.CountryId" class="btn-cinema">✏️ Редагувати</a>
            <a asp-action="Index" class="btn-secondary">Назад</a>
        </div>
    </div>

</div>