@model Kino.Models.Person

@{
    ViewData["Title"] = "Деталі персони";
}

<div class="container" style="margin-top: 50px;">
    <div class="cinema-form-card" style="max-width: 500px;">
        <h2 style="text-align: center; margin-bottom: 30px;">Картка актора</h2>

        <div style="text-align: center; margin-bottom: 20px;">
            <div style="font-size: 4rem;">👤</div>
        </div>

        <div class="form-group">
            <label class="control-label">ID</label>
            <input value="@Model.PersonId" class="form-control" readonly style="color: #666; width: 100px; margin: 0 auto;" />
        </div>

        <div class="form-group">
            <label class="control-label">Повне ім'я</label>
            <input value="@Model.FullName" class="form-control text-center" readonly style="font-weight: bold; font-size: 1.2rem;" />
        </div>

        <div style="
